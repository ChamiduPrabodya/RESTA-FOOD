PORT=5000
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=dev_secret_change_me

# Admin account (required)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Google Identity
# Comma-separated list is allowed (e.g. web + android client IDs)
GOOGLE_CLIENT_ID=

# Optional (currently not used by this backend's file-based stores).
# Provide your MongoDB Atlas connection string if/when you migrate persistence to MongoDB.
MONGODB_URI=
MONGODB_DB_NAME=
